# Presets (Domain-agnostic)

Preset = meta-metadata + default limits + default functions + default policies.

- preset-generic-min
- preset-generic-full
- preset-security-hardened
- preset-ui-friendly

Each preset is defined as JSON/YAML payloads under this folder.

# Notes

Minimal valid Error requires code, message, and severity. The code MUST be
in ERROR_CODE_REGISTRY.md. severity MUST be one of: info, warn, error, fatal.
Runner validates against error.schema.json.

This fixture asserts:

- `required: true` fields produce a deterministic validation error when missing.
- `json_schema` and `ui_schema` are deterministic and stable.
- `field_state` is returned for each field.

